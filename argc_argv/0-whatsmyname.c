#include "main.h"
#include <stdio.h>

/**
*main - Entry point
*@argc: the number of argument
*@argv: the array with the argument
*Return: Always 0
*/

int main(int argc, char *argv[])
{
	argc = 0;
	printf("%s\n", argv[argc]);

	return (0);
}
